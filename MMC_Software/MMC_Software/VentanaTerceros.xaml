<UserControl x:Class="MMC_Software.VentanaTerceros"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MMC_Software"
             mc:Ignorable="d" 
             d:DesignHeight="420" d:DesignWidth="780">

    <Grid Background="#FAFAFA" Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Encabezado -->
            <RowDefinition Height="Auto"/>
            <!-- Barra de búsqueda -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <TextBlock Grid.Row="0"
                   Text="Lista de Terceros"
                   FontSize="22"
                   FontWeight="Bold"
                   Foreground="#333"
                   Margin="0,0,0,12"/>

        <!-- Barra de búsqueda -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox x:Name="txtBuscar"
                     Width="240"
                     Height="36"
                     FontSize="15"
                     VerticalContentAlignment="Center"
                     Padding="8,0"
                     BorderBrush="#CCC"
                     BorderThickness="1"
                     Background="White"
                     ToolTip="Escriba para buscar"/>

            <Button Content="Recargar Datos" 
                    Width="150" Height="36"
                    FontSize="15"
                    Background="#1976D2" Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Margin="8,0,0,0"
                    Click="Button_Click"/>

            <Button x:Name="btnCrearTercero"
                    Content="👤 Crear"
                    Width="110" Height="36"
                    FontSize="15"
                    Background="#388E3C" Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Margin="8,0,0,0"
                    Click="btnCrearTercero_Click"/>

            <Button Content="🚪 Salir"
                    Width="110" Height="36"
                    FontSize="15"
                    Background="#D32F2F" Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Margin="8,0,0,0"
                    Click="ButtonCerrar_Click"/>
        </StackPanel>

        <!-- Tabla de terceros -->
        <DataGrid Grid.Row="2"
                  MouseDoubleClick="dgTerceros_MouseDoubleClick"
                  x:Name="dgTerceros"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  FontSize="15"
                  RowHeight="34"
                  GridLinesVisibility="None"
                  BorderBrush="#DDD"
                  BorderThickness="1"
                  HeadersVisibility="All"
                  AlternatingRowBackground="#F5F5F5">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="FontSize" Value="15"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Foreground" Value="#444"/>
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding TercerosID}" Width="50"/>
                <DataGridTextColumn Header="Tipo Doc" Binding="{Binding TercerosTipoDocumento}" Width="70"/>
                <DataGridTextColumn Header="Identificación" Binding="{Binding TercerosIdentificacion}" Width="110"/>
                <DataGridTextColumn Header="Nombre Completo" Binding="{Binding TercerosNombreCompleto}" Width="200"/>
                <DataGridTextColumn Header="Razón Social" Binding="{Binding TerceroRazonSocial}" Width="220"/>
                <DataGridTextColumn Header="Departamento" Binding="{Binding DepartamentoNombre}" Width="140"/>
                <DataGridTextColumn Header="Ciudad" Binding="{Binding CiudadNombre}" Width="140"/>
                <DataGridTextColumn Header="Dirección" Binding="{Binding TercerosDireccion}" Width="180"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding TercerosTelefono}" Width="110"/>
                <DataGridTextColumn Header="Email" Binding="{Binding TercerosEmail}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
