<UserControl x:Class="MMC_Software.VentanaCreacionAlmacen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MMC_Software"
             mc:Ignorable="d" 
             d:DesignWidth="800" Height="300">

    <UserControl.Resources>
        <!-- ✅ Estilo empresarial estándar -->
        <Style x:Key="BotonEmpresarialEstandar" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect Color="Black"
                                                  Opacity="0.25"
                                                  BlurRadius="8"
                                                  ShadowDepth="2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Black"
                                                          Opacity="0.35"
                                                          BlurRadius="12"
                                                          ShadowDepth="4"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.03" ScaleY="1.03"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#BDC3C7"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Opacity="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 🎨 Colores corporativos -->
        <SolidColorBrush x:Key="ColorCrear" Color="#4A90E2"/>
        <SolidColorBrush x:Key="ColorModificar" Color="#6C63FF"/>
        <SolidColorBrush x:Key="ColorGuardar" Color="#2ECC71"/>
        <SolidColorBrush x:Key="ColorEliminar" Color="#E74C3C"/>
        <SolidColorBrush x:Key="ColorSalir" Color="#34495E"/>
    </UserControl.Resources>

    <!-- 🧱 CONTENIDO PRINCIPAL -->
    <Grid Background="#F4F6F8" Margin="20" VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="🏬 Creación de Almacén"
                   Grid.Row="0"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="#2B3E5C"
                   Margin="10"
                   HorizontalAlignment="Left"/>

        <!-- Formulario -->
        <Border Grid.Row="1" 
                CornerRadius="12" 
                Padding="15" 
                Background="White"
                Margin="10"
                BorderBrush="#E0E0E0"
                BorderThickness="1">
            <Grid x:Name="BaseFormulario">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Código Almacén:" Margin="5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1" Margin="5" Name="txtCodigoAlmacen" IsEnabled="False"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Nombre Almacén:" Margin="5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="1" Grid.Column="1" Margin="5" Name="txtNobreAlmacen" IsEnabled="False"/>

                <!-- ListBox -->
                <ListBox Name="listboxAlmacenes" 
                         Grid.Row="0" Grid.RowSpan="3" 
                         Grid.Column="2"
                         Margin="5"
                         BorderBrush="#BDC3C7"
                         BorderThickness="1"
                         MouseDoubleClick="listboxAlmacenes_MouseDoubleClick"/>
            </Grid>
        </Border>

        <!-- Botones -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="➕"
                    Background="{StaticResource ColorCrear}"
                    Style="{StaticResource BotonEmpresarialEstandar}"
                    Click="ButtonCrear_Click"/>

            <Button Content="✏️ "
                    Background="{StaticResource ColorModificar}"
                    Style="{StaticResource BotonEmpresarialEstandar}"
                    Click="ButtonModifica_Click"/>

            <Button Content="💾"
                    Background="{StaticResource ColorGuardar}"
                    Style="{StaticResource BotonEmpresarialEstandar}"
                    Click="ButtonGuardar_Click"/>

            <Button Content="🗑"
                    Background="{StaticResource ColorEliminar}"
                    Style="{StaticResource BotonEmpresarialEstandar}"
                    Click="ButtonEliminar_Click"/>

            <Button Content="🚪 Salir"
                    Background="{StaticResource ColorSalir}"
                    Style="{StaticResource BotonEmpresarialEstandar}"
                    Click="ButtonCerrar_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
