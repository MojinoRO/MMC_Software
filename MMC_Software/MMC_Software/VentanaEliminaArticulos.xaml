<Window x:Class="MMC_Software.VentanaEliminaArticulos"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MMC_Software"
        mc:Ignorable="d"
        Title="Eliminar Artículos"
        Height="150" Width="200"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#f5f5f5"
        WindowStyle="None">

    <Window.Resources>
        <!-- Color base azul petróleo -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#2C3E50"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#2C3E50"/>
        <SolidColorBrush x:Key="TextColor" Color="White"/>
        <SolidColorBrush x:Key="BorderGray" Color="#CCCCCC"/>

        <!-- Estilo de botones -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource SecondaryColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#003E4A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <!-- Estilo para TextBlock -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{StaticResource BorderGray}" BorderThickness="1" CornerRadius="8" Padding="10" Background="{StaticResource PrimaryColor}">
        <StackPanel>
            <!-- Contenido principal -->
            <GroupBox Header="Rango de Ítems" FontWeight="Bold" Foreground="{StaticResource TextColor}" BorderBrush="{StaticResource TextColor}">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Item Desde:" Grid.Row="0" Grid.Column="0" Margin="0,0,8,0"/>
                    <TextBox x:Name="txtDesde" Grid.Row="0" Grid.Column="1"
                             Height="20" Width="40"
                             PreviewTextInput="txtDesde_PreviewTextInput"
                             HorizontalContentAlignment="Center"
                             KeyDown="txtDesde_KeyDown"/>

                    <TextBlock Text="Item Hasta:" Grid.Row="1" Grid.Column="0" Margin="0,5,8,0"/>
                    <TextBox x:Name="txtHasta" Grid.Row="1" Grid.Column="1"
                             Height="20" Margin="0,5,0,0" Width="40"
                             PreviewTextInput="txtDesde_PreviewTextInput"
                             KeyDown="txtHasta_KeyDown"/>

                    <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Aceptar" x:Name="btnAceptar" Width="70" Margin="0,0,8,0" Click="btnAceptar_Click"
                                 Background="#1ABC9C"/>
                        <Button Content="Cancelar" x:Name="btnCancelar" Width="70" Click="btnCancelar_Click"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </StackPanel>
    </Border>
</Window>
